# 需求

## 目标

先生成候选结果，再由独立验证者判定；失败时有界修复，并演示暂停恢复与耗尽终态。

## 输入与输出

首次验证返回 FAIL，修复后再次验证返回 PASS，最终输出 candidate-fixed；测试同时验证暂停恢复与耗尽失败。

## 非目标

- 不连接生产数据或生产接口
- 不替代业务、法律或安全负责人
- 不把 mock Provider 的结果当作真实模型认证
