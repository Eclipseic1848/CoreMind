# Child Run 产品面覆盖现有四入口但不包含 Web

`0.7.0` 的 Child Run 产品面覆盖 Config、Delegation Tool、Protocol v2、CLI、TUI、TypeScript SDK 与 Python SDK：四个现有用户入口读取同一 Child Run Projection，并按各自交互形态提供类型化事件、详情和控制；TUI 纳入本期，Web 明确不纳入。本期也不提供脱离活动父 Run 的独立 spawn、list、resume 或 detach 产品路径。
