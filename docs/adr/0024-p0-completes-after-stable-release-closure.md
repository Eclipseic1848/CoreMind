# P0 只在 0.7.0 稳定发布闭环后完成

P0 的完成条件是同一个 `0.7.0` 候选提交已通过 PR 合入受保护的 `main`，工程 CI、完整发布资格测试、真实 Provider 父子 Agent 验收以及 npm/PyPI 候选包安装验证全部通过，并由该提交发布 `v0.7.0` Tag、GitHub Release、npm 和 PyPI，最后从公开渠道重新安装并验证版本与基本功能。任一门禁失败都停止后续发布步骤，代码写完、局部绿测、合并或只创建 Tag 均不能单独称为 P0 完成，因为稳定版承诺必须与用户实际取得的产物一致。
